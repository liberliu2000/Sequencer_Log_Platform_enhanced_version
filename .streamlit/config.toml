[theme]
base = "light"
primaryColor = "#052659"
backgroundColor = "#F7FBFF"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#021024"
font = "sans serif"

[server]
# Streamlit requires a numeric ceiling here, so use a very large value.
maxUploadSize = 10240
